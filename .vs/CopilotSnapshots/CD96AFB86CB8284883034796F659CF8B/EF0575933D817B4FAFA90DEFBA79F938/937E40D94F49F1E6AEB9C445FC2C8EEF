#pragma once
#include "HelpfulFunc.h"
#include "Camera.h"

// 将来的にマップチップの配列などを持つ可能性を見据えてクラス化します
class Map
{
private:
    Image mapImageUsually; // 通常（1-1）用
    Image mapImageDebug;   // デバッグ（demo_1-1）用

public:
    void Init();
    void Render(Camera& camera); // 描画のためにカメラの情報を教えてもらう
};